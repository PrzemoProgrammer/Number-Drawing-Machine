import Container from "../../../components/Container";

// The class responsible for holding all the balls that are in the pool
class BallsStorage extends Container {
  constructor() {
    super();
  }
}

export default new BallsStorage();
