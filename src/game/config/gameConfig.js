export const MAX_BALLS_COUNT = 15; //max number of balls in the pot
export const BALL_TEXTURES = ["red_b", "yellow_b", "blue_b"]; //texture key images color balls
export const NUMBER_TEXTURE_KEY = "n"; //the name of the image of the number to which the number is added at a later stage
export const BALL_OFFSET = 50; //distance from the center of the ball. Needed to use in physics to make the ball bounce from its ends
export const BALL_MAX_ANGLE = 360; //the maximum possible spin on the ball when assigning values to them during creation
